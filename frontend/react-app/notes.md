Note: this folder contains a Vite React app.

To build and serve with Docker Compose (from repository root):

```bash
docker-compose build
docker-compose up
```

Frontend will be available at http://localhost:3000 and backend at http://localhost:8000

If you prefer to run the frontend dev server during development:

```bash
cd frontend/react-app
npm install
npm run dev
```
